#lang info

(define name "bit-struct")
(define collection "bit-struct")
(define scribblings '(("manual.scrbl" ())))

(define blurb '("Binary structs with conversion to/from bytes."))
(define primary-file "main.rkt")
(define homepage "https://github.com/jpverkamp/bit-struct/")

(define version "1.0")
(define release-notes
  '("Initial release."))

(define required-core-version "5.3")
